<UserControl x:Class="View.Views.AppointmentViews.AppointmentFilterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:View.Views.AppointmentViews"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="300">
    <Grid>
        <StackPanel>
         
            <TextBlock Text="Select filter timespan"/>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <RadioButton Grid.Column="0"
                             Content="Day" 
                             GroupName="IntervalRB"
                             IsChecked="{Binding IsDayVM}"/>
                <RadioButton Grid.Column="1"
                             Content="Week"
                             GroupName="IntervalRB"
                             IsChecked="{Binding IsWeekVM}" />
                <RadioButton Grid.Column="2"
                             Content="Month"
                             GroupName="IntervalRB"
                             IsChecked="{Binding IsMonthVM}" />
            </Grid>
            <DatePicker Text="{Binding AppointmentDateVM, Mode=TwoWay}"/>
        </StackPanel>
    </Grid>
</UserControl>
